**我的结论是：保留单一共享 SSM，也保留 \(G_f/\omega_f/\zeta_f\) 参数化，但不建议直接按附件执行完整 Step 5。应先增加一个规模很小、优先级最高的“生成模型—推断器一致性”阶段。**

这次核对后，最重要的发现不是“还需要更强的生理约束”，而是：

> **目前的负结果混合了参数可辨识性、生成模型失配、近似似然失准和不合适的判定门。必须先把它们拆开，才能判断参数贴边究竟需要通过哪一种调整解决。**

这不需要引入 shared-private SSM、systemic latent、多脑区状态或者新的空间前向模型，也不需要改变项目的核心创新叙事。

以下审查以仓库 `main` 的 **2026 年 9 月 3 日提交 `d9f9bfd`** 和你上传的 Step 5 修订案为依据。实验数值来自仓库报告；我没有在本轮重新运行原始数据或本地 Git-ignored 实验产物。

---

## 一、当前开发进度：已经不是“继续寻找合适参数范围”的阶段

仓库首页仍显示早期 synthetic smoke 状态，但结果索引已经记录了 Step 2–4。因此，不能依据首页判断当前科学进度，需要以具体实验报告为准。

| 阶段                  | 已经得到的证据                                                                                                          | 对下一步的约束                                    |
| ------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| 当前 T3a 实现           | 已有六状态 Balloon 核心、正值变换、Student-t IRLS 更新和固定区间平滑；代码明确说明其后验是工程近似，不是精确 Student-t 后验。                                 | 不能把优化成功或状态为正直接等同于统计推断正确。                   |
| Step 2：原始参数可辨识性     | `subject_03/13` 的 \(\kappa,\tau\) 触边；`subject_10` 三个自由参数均触边。部分案例存在参数不同、状态相近的近优解。                                 | 不支持继续增加原始参数自由度；但也没有证明所有案例都具有稳定共享状态。        |
| Step 3：跨 session 拟合 | 主候选相对固定模型的 \(\Delta\mathrm{NLL}=+5.288570\)，95% CI 为 \([1.319282,9.417574]\)，方向更差；7/18 被试至少一次有效 \(\kappa\) 接近边界。 | 当前结果不支持“被试级 \(\kappa\) 适应能改善跨 session 预测”。 |
| Step 4：综合坐标合成实验     | 两个一维方向都失败，但点估计边界率为 0，3840 次 multistart 全部成功；增益、时间方向仍存在系统性偏差和严重欠覆盖。                                               | **问题已经不只是贴边，也不只是优化器没有收敛。**                 |
| 附件 Step 5           | 仍是 planned：增加欠阻尼约束、参数后验传播、masked calibration，并区分参数级与状态级资格。                                                       | 方向可以保留，但必须修订推断验证和门控后再运行。                   |

上述实现和结果分别见模型代码、Step 2、Step 3、Step 4 报告及附件。

**所以，下一轮不应再以“找到一组不贴边的参数”为主要成功标准。** Step 4 已经展示了：参数可以不贴边、优化可以非常稳定，但参数推断仍然不可信。

真正需要解决的是：

$$
\boxed{\text{在既定单一共享 SSM 内，是否存在可信、稳定、有跨模态证据的 teacher 轨迹？}}
$$

这与“是否能够精确恢复每名被试的全部生理参数”不是同一个问题。

---

## 二、此次重审发现的三个关键问题

### 1. Step 5 的两个方向，其实已经在 Step 4 中测试过

Step 4 使用：

$$
g=\log\frac{\beta/\gamma}{\beta_0/\gamma_0},
\qquad
t=\log\frac{1/\sqrt{\gamma}}{1/\sqrt{\gamma_0}},
$$

并固定阻尼比。附件中的新坐标为：

$$
g=\log\frac{G_f}{G_{f0}},
\qquad
w=\log\frac{\omega_f}{\omega_{f0}}.
$$

因此，直接代入可得：

$$
\boxed{g_{\mathrm{new}}=g_{\mathrm{old}},\qquad w_{\mathrm{new}}=-t_{\mathrm{old}}.}
$$

在相同参考值、支持域、先验和推断器下：

| Step 5 候选 | 与已有实验的关系                                         |
| --------- | ------------------------------------------------ |
| `U1_G`    | 与 Step 4 的 `C1_G` 是同一个参数方向。                      |
| `U1_W`    | 与 `C1_T` 是同一个方向，仅改变坐标符号。                         |
| `U1_Z`    | 在固定 \(\omega_f\) 下，仍然等价于只改变 \(\kappa\)，但缩小了允许范围。 |
| `U3_GWZ`  | 才新增了阻尼与增益、频率联合变化的诊断。                             |

Step 4 的定义、逆变换和附件的新参数化支持这一对应关系。

在参考 \(\gamma_0=0.32\) 下，新阻尼域 \(\zeta_f\in[0.20,0.95]\) 对应：

$$
\kappa\in[0.2263,1.0748].
$$

所以 `U1_Z` 的实质变化是：把原来可能进入过阻尼区的 \(\kappa\) 搜索截断，而不是创造了一个新的可辨识方向。

**这并不意味着重参数化没有价值。** 它使增益、时间尺度和阻尼的含义更清晰，也可能改善数值条件。但可逆坐标变换不能凭空增加信息：若变换 Jacobian 可逆，Fisher 信息在新坐标下为 \(J^\top I J\)，其秩不会改变。

我的建议是：**保留 GWZ，但在计划中明确写成“解释清晰的坐标体系”，而不是预期独立解决参数恢复失败的机制。** 重新运行 `U1_G/U1_W` 必须有明确的新因素，例如修正生成合同或似然计算，否则主要是在重复 Step 4。

还有一个值得澄清的细节：**欠阻尼不等于数据中必须呈现明显周期。** 对这个二阶子系统，\(\zeta=0.95\) 的单位阶跃超调比例按公式计算仅约 \(0.007\%\)，虽然数学上仍欠阻尼，响应已经几乎看不出振荡。因此，“没有越过 1”不能代替响应形态和时间尺度检查。附件也已经正确说明，这些边界是本轮模型约束，不是普适医学正常范围。

### 2. Step 4 的生成器与拟合器不是同一个完整随机模型

这是我认为**最应该先处理的源码级问题**。

在 `experiments/evaluate_t3c_composite_synthetic_t2.py` 中，生成器的 `_driver()` 先生成衰减随机过程，再额外加入幅度为 `0.12` 的高斯形脉冲；随后 `_truth_trial()` 用这个外部驱动，通过独立 `solve_ivp` 积分五个血流状态。血流状态生成过程本身没有加入拟合器配置中的各状态过程扩散。

而拟合器采用：

$$
\dot r=-0.45r
$$

作为驱动先验的确定性部分，并给六个变换状态配置过程噪声；其中 \(s,\log f,\log v,\log p,\log q\) 的过程噪声均非零。拟合合同没有把生成器的脉冲作为相同生成规律纳入。

两者共享 Balloon 方程，并不意味着它们共享同一个联合分布：

$$
p(\phi,x_{0:T},y_{0:T}).
$$

这影响了“SBC 失败”的解释。标准 simulation-based calibration 验证的是：当参数和数据从所声明的同一个概率模型生成时，推断算法能否恢复该模型的后验。它不是在任意已知真值仿真上检查覆盖率的统称。([arXiv][1])

因此，当前合成结果可能同时包含：

$$
\text{生成—拟合模型失配}
+\text{近似推断误差}
+\text{有限信息导致的参数不确定性}.
$$

**现在还不能从中单独确定每一部分的贡献。** 尤其不能据此直接认定：“即使没有真实生理噪声，参数本身也必然不可辨识。”

我不是建议推翻 Step 4 的负判定。原合同的 `FAIL`、不晋级结论和全部产物应原样保留。应在新版本增加一条审计说明：

> 既有失败验证了当前完整生成—拟合管线未通过注册门，但尚未隔离参数可辨识性、生成模型失配与后验近似误差。

**独立生成器仍然要保留；需要一致的是概率模型，不是必须复用同一段代码。**

### 3. 当前“参数似然”实际上是边际预测评分近似

`src/inference/t3a_balloon_robust_ssm.py` 的 `_observation_update()` 在状态更新前，用预测均值和各坐标的边际方差，构造 Student-t 预测尺度，然后逐坐标累加 log density。代码本身明确说明：这是高斯状态不确定性与 Student-t 观测噪声卷积的近似。

之后，Step 4 的 `_nll_function()` 直接把这个 `predictive_log_likelihood` 当作参数优化、profile 和网格后验的依据。

但完整 SSM 在一个时点的预测密度应为：

$$
p(y_t\mid y_{<t},\phi)
=
\int
\left[
\prod_m p(y_{m,t}\mid x_t,\phi)
\right]
p(x_t\mid y_{<t},\phi)\,dx_t.
$$

它一般不等于“把三个边际预测密度相乘”。即使给定状态后观测噪声独立，把共享状态积分掉以后，EEG、HbO、HbR 的预测仍可能相关。

这里应区分：

**作为预测评价指标：** 边际 proper score 可以有合理用途。

**作为参数后验的似然：** 必须验证它是否足够逼近目标模型的似然；否则得到的是近似或广义后验，而不是仅靠增加网格点就能变成的校准后验。

这也解释了为什么：

> 将 81 点网格改成 161 点，或者将参数样本从 256 增加到 512，可能提高数值积分精度，却不能修复被积分对象本身的偏差。

Step 4 的报告事实上已经观察到：增益方向的拟合参数在某些 held-out 评分上优于“生成真值参数”；报告也指出，候选可能在补偿近似推断，而不是恢复真实参数。

**我建议把 `parameter_log_likelihood` 与 `predictive_score` 明确拆开，而不是继续由同一个字段承担两种统计含义。**

---

## 三、怎样在不改变创新叙事的情况下处理贴边

首先，应把“参数贴边”当作**诊断信号**，而不是需要通过参数变换消灭的表面现象。

在当前项目中，我建议采用下面的判断方式：

| 观察到的情况                            | 应优先检验什么             | 不改变状态结构的处理                     |
| --------------------------------- | ------------------- | ------------------------------ |
| 不同起点得到不同局部解                       | 优化和数值误差             | 网格检查、标量区间搜索、积分步长与导数核验。         |
| profile 很平，参数随先验变化，但 teacher 轨迹稳定 | 数据是否只支持参数组合或状态      | 固定参数或对参数不确定性积分，不宣称参数被精确恢复。     |
| profile 单向朝边界改善                   | 观测尺度、驱动先验、过程噪声或模型失配 | 分解各模态评分，进行冻结的单因素敏感性诊断，而不是直接扩边。 |
| 合成同模型实验也出现明显定向偏差                  | 推断器和似然近似            | 先修推断；暂缓真实数据个体化参数拟合。            |
| 参数不贴边，但状态或区间不可信                   | “不贴边”是不是虚假的成功标准     | 拒绝晋级，继续检验状态与共享信息。              |

### 最值得保留的是“参数级结论与状态级 teacher 资格分离”

附件第 11 节是正确方向：参数无法用于个体生理解释，并不自动意味着所有状态轨迹都不能使用。

但还需要补上两项限制。

**第一，跨参数稳定，不等于接近真实状态。**

Step 4 中，预测等价替代解与最优解之间，driver correlation 最低为 `0.99914/0.99703`，driver NRMSE 最大为 `0.0791/0.0779`。这是值得继续研究的积极线索。可是这些指标比较的是**两个估计结果**，不是估计与生成真值。

两个估计可能共享同一种偏差。所以下一轮必须同时报告：

$$
\text{跨参数稳定性}
\quad\text{与}\quad
\text{对真实 }r(t)\text{、真实 clean observation 的恢复}.
$$

**第二，状态稳定，不等于其中确实包含两个模态的共享信息。**

一个主要由 EEG 或强驱动先验决定的 \(r(t)\)，也可能非常稳定。因此，teacher 资格不能仅靠“联合重建好看”和“参数扰动后相关系数高”，仍须检查另一模态是否带来超过自身历史、任务时序和配对 null 的增量。

这与仓库已经冻结的研究问题一致：能够构造联合坐标、单模态能够预测坐标，都不自动证明该坐标具有物理充分性和可重复的跨模态增量。

### 固定参数并不破坏你的创新叙事

你真正需要保护的是：**通过生理约束的共享动态过程构造 teacher，并检验独立模态表示所保留的生理相关信息及后续耦合增量。**

它不要求每个被试都有一组可解释的自由生理参数。仓库也明确将 \(r\) 定义为固定 gauge 下的 operational shared neural state，而不是测得的放电率。

因此，若自由拟合不能提供稳定增量，而 `U0_FIXED` 在充分的参数敏感性、状态恢复和 null 检验下合格，保留固定参数 teacher 是科学上合理的收缩，不是放弃创新点。

不过，`U0_FIXED` 也不能因为“没拟合参数，所以参数方差为零”而天然通过稳健性门。它仍应接受预设 GWZ 情景下的敏感性检验；这些情景不是新的被试参数拟合。

---

## 四、附件 Step 5 需要修改的具体内容

### 1. 不要在本轮同时切换 tokenizer 的监督目标

这是一个与“叙事不变”直接相关的冲突。

仓库当前计划要求 `source` 保留**同模态的联合 teacher 轨迹切片**；方法合同也规定 `source` 表示同模态连续 teacher target。附件第 11.2 节却写成，资格只支持用 \(\bar r(t)\) 及其不确定性监督 tokenizer。

这两种目标不能默认等价：

$$
\begin{aligned}
T_E(t)&=\mathbb E[h_E(x_t)\mid D],\\
T_F(t)&=\mathbb E[h_F(x_t)\mid D]
\end{aligned}
$$

与

$$
T_E(t)=T_F(t)=\mathbb E[r_t\mid D]
$$

是不同的学习问题。后者把 fNIRS 的逆动力学负担转移给 tokenizer，也改变了原来观测时间轴上的 lag 解释。

**本轮建议维持仓库的同模态观测空间 teacher target，将 \(r(t)\) 作为共享过程诊断和额外导出，不同时启动 tokenizer 目标改版。** 否则，即使结果改善，也很难判断究竟来自 SSM 修复还是监督任务改变。

### 2. 保留支持域，但必须补齐先验与边界诊断

附件给了支持域，却尚未完整指定新参数后验所用的先验分布。仅有范围不足以定义 `prior × likelihood`。

应明确先验定义在 \(g,w,\zeta\) 还是 \(g,w,z\) 上，以及转换密度和网格积分时如何处理 Jacobian。不能因为优化使用 logistic 坐标，就无意中改变阻尼比的先验。

同时，logistic 变换下有限 \(z\) 几乎不会精确产生 \(\zeta=0.95\)，所以“等于上界的比例”不是有效边界诊断。应检查：

* 距离边界的标准化距离及预设边界带；
* 边界带中的后验概率质量；
* likelihood-only 最优点与加入先验后的最优点是否分离。

这些阈值应在新合同中冻结。**参数被先验拉回内部，可以是合理正则化，但不能被称为数据恢复了内部生理参数。**

另外，“大量靠近 0.95 就判定为模型失配”应改成更审慎的表述：它是模型失配、信息不足或约束与数据冲突的警报，需要诊断；单靠边界接触本身不能区分原因。

### 3. 将真正的 SBC 与失配压力测试分开

建议将合成实验拆成两个明确命名的集合：

**`matched_model_calibration`：** 参数先验、初始状态、状态转移、过程噪声、观测噪声全部一致，用于验证推断算法及后验校准。

**`misspecification_stress_test`：** 保留当前脉冲、不同过程噪声、额外污染等条件，用于检验模型失配下状态是否稳健。

两者都重要，但不能共用“SBC 通过/失败”这一解释。

还要注意，**SBC 通过也不证明参数可辨识**：它验证推断是否与目标后验一致；一个信息很少、后验很宽的问题仍可以校准。SBC 本身的测试量选择也会影响检出能力。([arXiv][2])

### 4. “存在预测等价替代参数”不宜继续作为普适的一票否决门

Step 4 的门要求：不能存在相差至少注册跨度 1%、仍在似然支持域内、预测差异很小的替代参数。这是该轮已经冻结的判定，原结果应保留。

但从新实验设计角度看，这很接近要求“有限噪声下参数必须接近点识别”。一个正常的有限宽度置信区间，本来就会包含若干预测相近的参数值。

下一版应改为检查：参数区间是否小于预先定义的**有实质意义的变化尺度**、是否对边界过度敏感，以及这种参数不确定性是否影响 teacher 目标。参数置信与预测置信本来就可以不同，相关 profile-likelihood 方法也专门区分这两层问题。([PubMed Central (PMC)][3])

这不改变 Step 4 的负结论，因为那一轮还有严重覆盖不足和定向偏差；只是避免下一轮继续被一个不恰当的硬门卡住。

### 5. 必须分别校准“潜在轨迹”与“未来带噪观测”

附件采用全方差公式拆分参数、状态和观测不确定性，基本思路正确。

但 tokenizer teacher 的目标若是 clean trajectory，对应的不确定性应为：

$$
V^{\mathrm{teacher}}
=
\mathbb E_\phi\!\left[
\operatorname{Var}(h(x)\mid D,\phi)
\right]
+
\operatorname{Var}_\phi\!\left[
\mathbb E(h(x)\mid D,\phi)
\right].
$$

预测未来带噪观测时，才再加：

$$
V^{\mathrm{observation}}
=
\sigma^2\frac{\nu}{\nu-2}.
$$

**观测覆盖率校准，不等于 \(r(t)\) 或 clean trajectory 的可信区间校准。** 把观测噪声设得很大，可以使区间覆盖观测，却未必使潜在状态更可信。

因此，合成实验必须对真实 \(r(t)\)、clean EEG/HbO/HbR 单独做 coverage 和误差检查。真实数据没有这些真值，只能通过预测检验、敏感性和 null 提供间接证据。

此外，当前状态协方差包含过程随机性带来的不确定性；将其统一称为纯粹的 `state_epistemic_variance` 容易过度解释。更中性的主字段是 `state_posterior_variance`，并说明它条件于既定过程模型和固定超参数，不包含全部模型失配不确定性。

### 6. 调整几项过强或不充分的 UQ 门

| 附件中的安排                                    | 建议修改                                                                                     |
| ----------------------------------------- | ---------------------------------------------------------------------------------------- |
| predicted SD 与绝对误差必须显著正相关                 | 不应作为所有 teacher 的通用资格门。近似同方差且校准的模型可能没有可排序的风险差异；它更适合决定是否允许 precision weighting。            |
| 遮挡后不确定性不能下降                               | 改为匹配条件下的配对总体检验。不能要求非线性模型中每个时点、每个实现都单调增加。                                                 |
| coverage 使用二项区间                           | 不应把自相关时间点当独立试验；按 replicate、trial 或 subject 聚合，并使用匹配的聚类推断。                                |
| cross-fitted split-conformal              | 先明确目标是“已知被试的新 trial”还是“新被试”，明确校准单位和交换性假设；普通 cross-fit 本身不自动提供标准 split-conformal 的有限样本保证。 |
| 固定 161 点、256 个样本                          | 改为同时检查积分分辨率、尾部质量及采样有效性；点数不是后验准确性的保证。                                                     |
| `U3_GWZ` 使用截断 Laplace/importance sampling | 继续作为诊断，但须检查提议分布是否覆盖脊线、边界和可能的多峰，不能只比较 128/256 次结果是否接近。                                    |

时间相关或分布漂移下，conformal 的保证依赖额外条件，不能直接沿用交换样本结论。([arXiv][4])

### 7. 数据预处理和噪声尺度必须纳入“未读取 masked target”的检查

Step 3 报告已明确承认：上游 canonical loader 对完整记录做 median/MAD 标准化，使用了留出记录自身的全记录分布。因此它的结论是预标准化记录上的 target-masked 泛化，不是完全无目标记录上下文的部署预测。

Step 5 不能只确认 smoother 没看遮挡坐标。还应确认被遮挡值没有通过标准化、滤波、重采样或功率窗口进入可见输入。需要在会传播信息的处理前施加相应信息限制，或者明确采用独立的输入与评分处理路径。

附件用训练 trial 的 robust first-difference 估计噪声，是可以保留的初始方法，但不能直接当作已校准的观测噪声尺度。信号变化和时间相关噪声都会影响差分统计。建议先在已知噪声的合成数据上检查该估计器，再固定真实数据的使用规则。

---

## 五、我建议重排后的实验计划

不再扩大模型搜索，只改变实验的先后顺序和解释边界。

### 第一阶段：Step 5A0——先做推断一致性定位

**目标：回答“当前偏差来自哪里”，暂不回答被试差异，也不做完整不确定性森林图。**

| 检查               | 实施方式                                                     | 结果如何定位问题                     |
| ---------------- | -------------------------------------------------------- | ---------------------------- |
| 参数化等价检查          | 同一组 GWZ 与对应 \(\beta,\gamma,\kappa\) 使用同一前向核心，比较轨迹及数值导数。  | 排除重参数化实现错误。                  |
| `oracle_r_known` | 在明确的确定性血流生成合同下固定真实 \(r(t)\)，直接计算 Student-t 观测似然，不经过 EKF。 | 先检验前向映射和条件参数恢复。              |
| 完整同模型校准          | 从与拟合器一致的初始状态和随机转移模型生成数据，再运行推断。                           | 区分推断近似与数据本身信息不足。             |
| 小规模参考推断          | 在线性高斯特例中对照精确 Kalman 结果；对少量短的非线性案例使用误差可检查的参考推断。           | 判断现有 EKF/IRLS 与预测密度近似造成多大偏差。 |
| 原 Step 4 条件重放    | 保留脉冲和生成差异，作为单独的失配压力测试。                                   | 定位生成规律改变是否把最优参数系统性推走。        |

附件已经加入 `oracle_r_known`，这一项应保留，并提升为最先执行的诊断，而不是大型合成实验中的一个附属结果。

初期只需选取少量固定案例定位问题；定位并冻结修正后，再运行注册的 60 次独立重复。不要在统计对象尚不清楚时先完整铺开所有候选、噪声条件和参数采样。

### 第二阶段：Step 5A1——只验证最小候选能否成为状态 teacher

保留附件中的：

$$
U0,\quad U1_G,\quad U1_W,\quad U1_Z,
$$

以及仅诊断用途的 `U3_GWZ`。不引入新状态，不恢复 \(\tau,\alpha,E_0,P_0,Q_0\) 自由拟合。

这一阶段的主要端点应是：**真实状态与 clean trajectory 恢复、teacher 不确定性、跨参数稳定性和共享信息 null。**

参数 coverage 仍报告，但它主要约束参数解释；风险排序主要约束不确定性加权；二者不要再不加区分地变成整个项目能否继续的唯一开关。

对于真实存在平坦方向的情况，不必强行追逐唯一最优点。可以固定该方向，或者在经过验证的参数分布上积分。重要的是坦诚报告“数据对该参数提供的信息有限”，并检验 teacher 是否仍稳健。

### 第三阶段：Step 5B——真实数据先做最小状态检验

保留 subjects 01–18 的开发边界及三个 session 的数据范围。先比较固定模型与合成阶段合格的最简单单参数候选，不立即给所有被试运行完整 U3 后验。

这里有一个实验解释变化需要写清：附件采用每个 session 8 个训练 trial、2 个 held-out trial，且三个 session 都参与同一被试参数拟合。**它检验的是已有 session 中的新 trial，不是 Step 3 那种整 session 留出的泛化。** 两者都可以研究，但不能互相替代。

真实数据阶段优先看：

**masked 预测是否有效，另一模态是否带来超过自身历史与配对 null 的增量，以及这些结论是否依赖边界附近参数或某一个被试。**

中心 4 秒遮挡可以保留，但不宜成为证明跨模态共享性的唯一证据；附件里的整模态缺失诊断以及与自身历史基线的比较同样重要。

subjects 19–23 继续只能称为算法冻结后的 replication，不能称为完全未参与开发的新确认队列；仓库已明确说明这一历史边界。subjects 24–29 保持关闭。

### 第四阶段：核心 teacher 合格后，再完成全面 UQ 分析

你要求的跨模态、跨被试不确定性比较可以完整保留，但应置于核心状态资格之后。否则很容易得到大量漂亮的方差分解、ICC 和森林图，却仍不知道被比较的后验是否有意义。

tokenizer 暂时只导出 mean、variance、mask；先使用统一权重。只有 teacher 不确定性具有经过验证的目标相关性和风险排序能力后，才测试 clipped precision weighting。这一点附件已经安排得合理。

---

## 六、对当前研发工作的明确建议

**下一项开发任务应是一个独立版本的 `Step5A_inference_consistency`，而不是直接启动整个 `GWZ uncertainty measured` 实验族。**

它只需要围绕现有代码完成三个实质改动：

1. **分开同模型校准与失配压力测试**，让每个合成实验具有明确的完整生成分布。
2. **分开参数推断使用的似然与预测评价使用的近似评分**，先量化现有 EKF/IRLS 管线的误差。
3. **补齐对真实 \(r(t)\) 和 clean observation 的恢复与覆盖检验**，而不仅比较不同拟合结果彼此是否相似。

旧 Step 1–4 runner、配置、结果和负判定不覆写；新诊断追加版本。同步更新研究状态入口，避免首页继续将当前进展显示为早期 smoke。tokenizer 的监督目标、独立模态输入所有权和耦合评价合同均不在这一轮改动。

**最终判断是：你的创新叙事可以保持，但需要把研发目标从“让自由参数不再贴边”改为“验证并获得不依赖不可信参数适应的共享 teacher”。**

当前最有希望的路线不是继续放开更多参数，也不是仅靠 \(\zeta<1\) 把解挡回去，而是：

$$
\boxed{
\text{先隔离推断误差}
\;\longrightarrow\;
\text{只保留数据支持的自由度}
\;\longrightarrow\;
\text{用状态恢复、稳健性与跨模态增量证明 teacher}
}
$$

这条路线保留你的单一共享生理 SSM，也保留后续 tokenizer 与耦合发现的研究主线；同时，它避免把整个项目的成败绑在“每名被试都必须恢复出一组唯一生理参数”这个目前没有得到证据支持的要求上。

[1]: https://arxiv.org/html/1804.06788v2 "Validating Bayesian Inference Algorithms with Simulation-Based Calibration"
[2]: https://arxiv.org/abs/2211.02383 "[2211.02383] Simulation-Based Calibration Checking for Bayesian Computation: The Choice of Test Quantities Shapes Sensitivity"
[3]: https://pmc.ncbi.nlm.nih.gov/articles/PMC3490710/?utm_source=chatgpt.com "Likelihood based observability analysis and confidence ... - PMC"
[4]: https://arxiv.org/abs/2202.13415?utm_source=chatgpt.com "Conformal prediction beyond exchangeability"
